import { createSlice } from '@reduxjs/toolkit'

export const initialState = {}
export const apiSlice = createSlice({
  name: 'apiSlice',
  initialState,
  reducers: {}
})

// this slice could not be continued due to shortage of time so moved the api logic to custom hook and worked around in order to save time
