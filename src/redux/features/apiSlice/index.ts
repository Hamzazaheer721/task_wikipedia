import { createSlice } from '@reduxjs/toolkit'

export const initialState = {}
export const apiSlice = createSlice({
  name: 'apiSlice',
  initialState,
  reducers: {}
})

// this slice could not be continued due to shortage of time
